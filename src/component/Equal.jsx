import React, { Component } from 'react';

class Equal extends Component {
    render() {
        return (
            <div>
                <button onClick={() => {equalRes}}>=</button>
            </div>
        );
    }
}

export default Equal;